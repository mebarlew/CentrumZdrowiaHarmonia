---
import { contactInfo } from '../data/contact';

const currentYear = new Date().getFullYear();

const quickLinks = [
  { href: '#o-nas', label: 'O nas' },
  { href: '#specjalisci', label: 'Nasz zespół' },
  { href: '#uslugi', label: 'Usługi' },
  { href: '#cennik', label: 'Cennik' },
  { href: '#kontakt', label: 'Kontakt' },
  { href: '/polityka-prywatnosci', label: 'Polityka prywatności' }
];
---

<footer class="bg-charcoal text-pebble py-16">
  <div class="max-w-7xl mx-auto px-6 sm:px-8 lg:px-12">
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-10 mb-12">
      <!-- Main info -->
      <div class="lg:col-span-2">
        <h3 class="text-lg font-display font-semibold text-sage-light mb-4">
          {contactInfo.name}
        </h3>
        <p class="text-sand/80 leading-relaxed mb-6 max-w-md">
          Wspólnie tworzymy bezpieczną przestrzeń, gdzie każdy czuje się wysłuchany,
          zrozumiany i doceniony.
        </p>
        <div class="flex gap-4">
          <a
            href={contactInfo.facebook}
            target="_blank"
            rel="noopener noreferrer"
            class="w-10 h-10 bg-sage/20 text-sage-light rounded-xl flex items-center justify-center hover:bg-sage hover:text-white transition-all duration-300"
            aria-label="Facebook"
          >
            <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
              <path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/>
            </svg>
          </a>
        </div>
      </div>

      <!-- Quick links -->
      <div>
        <h4 class="text-sm font-semibold text-white uppercase tracking-wider mb-5">Szybkie linki</h4>
        <ul class="space-y-3">
          {quickLinks.map((link) => (
            <li>
              <a
                href={link.href}
                class="text-sand/70 hover:text-gold transition-colors text-sm"
              >
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Contact -->
      <div>
        <h4 class="text-sm font-semibold text-white uppercase tracking-wider mb-5">Kontakt</h4>
        <address class="not-italic text-sm space-y-3">
          <p class="text-sand/70">
            {contactInfo.address.street}<br>
            {contactInfo.address.postalCode} {contactInfo.address.city}
          </p>
          <div class="space-y-1.5">
            {contactInfo.phones.map((phone: string) => (
              <p>
                <a
                  href={`tel:+48${phone.replace(/\s/g, '')}`}
                  class="text-sage-light hover:text-gold transition-colors"
                >
                  tel. {phone}
                </a>
              </p>
            ))}
          </div>
        </address>
      </div>
    </div>

    <!-- Bottom bar -->
    <div class="border-t border-stone/20 pt-8 text-center space-y-2">
      <p class="text-pebble text-sm">
        &copy; {currentYear} {contactInfo.name}. Wszelkie prawa zastrzeżone.
      </p>
      <p class="text-pebble/60 text-xs">
        Wykonanie: <a href="https://www.linkedin.com/in/jabarlew/" target="_blank" rel="noopener noreferrer" class="text-sage-light/70 hover:text-gold transition-colors">Bartosz Lewandowski</a> · <a href="mailto:mebarlew@gmail.com" class="text-sage-light/70 hover:text-gold transition-colors">mebarlew@gmail.com</a>
      </p>
    </div>
  </div>
</footer>
